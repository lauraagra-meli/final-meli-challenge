package meli.dh.com.finalmeliproject.model.enums;

/*
* ENUM para registrar o status do pedido, quando aberto ("OPENED"), o pedido ainda
* pode adicionar produtos, quando fechado ("CLOSED"), o pedido é finalizado
 */

public enum OrderStatus {
    OPENED,
    CLOSED
}
